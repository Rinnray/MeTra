import { dev } from '$app/environment';

export const title = 'MeTra';
export const description = 'A media tracker website built with SvelteKit';
export const url = dev ? 'http://localhost:5173' : 'https://metra.rinnray.dev';
